#include <bits/stdc++.h>

using namespace std;

#define scan(x) do{while((x=getchar())<'0'); for(x-='0'; '0'<=(_=getchar()); x=(x<<3)+(x<<1)+_-'0');}while(0)
char _;

int N, K, M, ans = 0;

inline void recurse(int n, int k, int m) {
    if (n == 0) ans++;
    // recurse for all possibilities

}

signed main() {
    cin.tie(0); cin.sync_with_stdio(0);
    cin >> N >> K >> M;



    return 0;
}
