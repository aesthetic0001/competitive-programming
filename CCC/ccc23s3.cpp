#include <bits/stdc++.h>
#define MAX 2000

using namespace std;

#define scan(x) do{while((x=getchar())<'0'); for(x-='0'; '0'<=(_=getchar()); x=(x<<3)+(x<<1)+_-'0');}while(0)
char _;

int v[MAX][MAX], N, M, R, C;

signed main() {
    cin.tie(0); cin.sync_with_stdio(0);
    cin >> N >> M >> R >> C;

    return 0;
}
