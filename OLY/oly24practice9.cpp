#include <bits/stdc++.h>

using namespace std;

int T;

signed main() {
    cin.tie(0); cin.sync_with_stdio(0);

    cin >> T;

    for (int i = 0; i < T; i++) {
        int N; cin >> N;
        for (int j = 0; j < N; j++) {
            int L, R; cin >> L >> R;
        }
    }

    return 0;
}
